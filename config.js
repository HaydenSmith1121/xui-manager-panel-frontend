// Runtime API configuration.
// Empty string keeps requests same-origin, which is recommended when Nginx proxies /api and /sub.
window.XUI_MANAGER_API_BASE_URL = window.XUI_MANAGER_API_BASE_URL || "";
